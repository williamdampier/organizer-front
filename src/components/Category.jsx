import React from 'react';
import PropTypes from 'prop-types';

const Category = props => {
    return (
        <div>
            
        </div>
    );
};

Category.propTypes = {
    
};

export default Category;