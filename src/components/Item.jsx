import React from 'react';
import PropTypes from 'prop-types';

const Item = props => {
    return (
        <div>
            
        </div>
    );
};

Item.propTypes = {
    
};

export default Item;