import React from 'react';
import PropTypes from 'prop-types';

const Section = props => {
    return (
        <div>
            
        </div>
    );
};

Section.propTypes = {
    
};

export default Section;